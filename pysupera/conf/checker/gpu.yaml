# RAPIDS cuML NearestNeighbors (brute-force L2, k=1).
# Requires: pip install cuml-cu12 cupy-cuda12x
name: gpu
# Rows of cloud_a per CuPy kernel launch in Stage-2 exact checks.
# Increase for larger GPU memory / smaller clouds; decrease to avoid OOM.
chunk_size: 512
